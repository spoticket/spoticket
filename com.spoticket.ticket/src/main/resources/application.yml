spring:
  application:
    name: ticket
  datasource:
    url: jdbc:postgresql://13.125.249.226:5432/spoticket
    username: spoticket
    password: spoticket
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: ticket_service
server:
  port: 8085

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka